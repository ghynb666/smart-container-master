* [x] Sa-Token 依赖已添加并成功解析。

* [x] `CustomerEntity` 已包含 `password` 字段。

* [x] `container-account` 能够成功注册新用户。

* [x] `container-account` 能够成功登录用户并返回 Token。

* [x] Redis 能够存储 Sa-Token 会话（如果 Redis 已启用）。

