# Smart Container (智能货柜系统)

## 📖 项目简介
Smart Container 是一个基于微服务架构的智能货柜管理系统。该系统旨在提供一套完整的解决方案，用于管理智能货柜设备的运行状态、处理用户订单、集成多种支付渠道，并实现与硬件设备的高效通信。

## 🏗️ 系统模块
项目采用 Maven 多模块构建，主要包含以下核心模块：

- **container-account**: 用户账户服务，管理用户信息及资金账户。
- **container-api**: 公共 API 接口定义，包含 Feign 客户端和 DTO 对象。
- **container-channel**: 渠道集成服务，对接支付宝、微信支付及短信通知等外部渠道。
- **container-common**: 公共模块，包含通用的工具类、常量、实体定义、异常处理及 Redis/RocketMQ 配置。
- **container-communicate**: 设备通信服务，负责与硬件设备进行交互（基于 Netty/MQTT 协议）。
- **container-device**: 设备服务，处理设备状态上报、开门指令及重量变化等核心业务。
- **container-manager**: 管理后台服务，提供运营管理功能。
- **container-order**: 订单服务，处理购物订单的创建、结算与状态流转。

## 🛠️ 技术栈

### 后端技术
- **开发语言**: Java 8
- **核心框架**: Spring Boot 2.7.7
- **微服务架构**: Spring Cloud 2021.0.5, Spring Cloud Alibaba 2021.0.4.0
- **持久层**: MyBatis, MySQL 8.0, Druid 连接池
- **消息队列**: RocketMQ
- **缓存**: Redis
- **工具库**: Hutool, FastJSON, Lombok

### 硬件通信
- **通信协议**: TCP (Netty), MQTT

## 🚀 快速开始

### 环境要求
- JDK 1.8+
- Maven 3.6+
- MySQL 8.0+
- RocketMQ
- Redis

### 部署步骤

1. **克隆项目**
   ```bash
   git clone https://github.com/ghynb666/smart-container-master.git
   ```

2. **初始化数据库**
   请在 MySQL 中执行 `DB/` 目录下的 SQL 脚本，初始化各模块所需的数据库表结构：
   - `container_account_db.sql`
   - `container_active_db.sql`
   - `container_coupon_db.sql`
   - `container_device_db.sql`
   - `container_order_db.sql`
   - `container_product_db.sql`

3. **配置环境**
   修改各模块 `src/main/resources` 下的配置文件（如 `bootstrap.yml` 或 `application.yml`），配置您的数据库、Redis 和 RocketMQ 连接信息。

4. **编译构建**
   在项目根目录下执行 Maven 构建命令：
   ```bash
   mvn clean install
   ```

5. **启动服务**
   按顺序启动核心服务模块（建议顺序）：
   - Common 依赖服务（如配置中心/注册中心，如果使用 Nacos）
   - container-account
   - container-device
   - container-communicate
   - container-order
   - container-channel
   - ...

## 📂 目录结构
```
smart-container-master/
├── DB/                      # 数据库初始化脚本
├── container-account/       # 账户服务模块
├── container-api/           # API 接口模块
├── container-channel/       # 渠道集成模块
├── container-common/        # 公共通用模块
├── container-communicate/   # 通信服务模块
├── container-device/        # 设备服务模块
├── container-manager/       # 管理后台模块
├── container-order/         # 订单服务模块
└── pom.xml                  # 父工程 Maven 配置
```

## 🤝 贡献
欢迎提交 Issue 和 Pull Request 来改进本项目。
